export enum DICE_TILES_TYPES {
  ATTACK = "ATTACK",
  SHIELD = "SHIELD",
  MAGIC = "MAGIC",
}

export type DICE_VALUE_TYPE = {
  index: number;
  value: number;
  name: string;
};

export enum DICE_TILES_POWERS_TYPES {
  HEALTH = "HEALTH",
}
